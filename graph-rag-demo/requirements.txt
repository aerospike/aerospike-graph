# Phase 1: Core dependencies
gremlinpython>=3.6.0,<3.8.0  # Compatible with Aerospike Graph (avoid 3.8+ breaking changes)
requests>=2.31.0              # HTTP client for Ollama

# Phase 2: Document parsing
PyMuPDF>=1.24.0               # PDF support

# Phase 3: Vector search
pymilvus>=2.4.0               # Milvus Lite for vector storage

# Phase 8: UI (install later)
# streamlit>=1.30.0
